# FeeSetu V14
Professional touch-first student fee payment kiosk for Gov PG College Shivpuri.

Run with `npm install` then `npm start`. Deploy with the supplied Render configuration.

See `MASTER_PROMPT.md` for the final specification.
