# FeeSetu Responsive Fix

This build adjusts the kiosk layout dynamically for:
- Large desktop kiosk / TV displays
- Laptop screens
- Tablet screens
- Android/mobile screens
- Short-height screens

The layout uses `100dvh`, clamp-based sizing, media queries and a fixed 3-column keypad so the keyboard remains visible without page scrolling.
